# 快速启动指南

## 🚀 5分钟快速上手

### 方式一：自动化安装（推荐）

1. **运行配置向导**
   
   双击运行 `setup.bat` 文件，它会自动完成所有环境配置。

2. **配置 API Key**
   
   编辑 `backend\.env` 文件：
   ```env
   OPENAI_API_KEY=sk-your-actual-api-key-here
   ```

3. **启动系统**
   
   双击运行 `start_all.bat` 文件

4. **开始使用**
   
   浏览器将自动打开 http://localhost:3000

### 方式二：手动启动

#### 启动后端

1. 双击运行 `start_backend.bat`
2. 等待后端服务启动完成

#### 启动前端

1. 双击运行 `start_frontend.bat`
2. 等待前端编译完成
3. 浏览器访问 http://localhost:3000

## 📋 前置要求

在开始之前，请确保已安装：

- ✅ **Python 3.10+**
  ```bash
  python --version
  ```

- ✅ **Node.js 18+**
  ```bash
  node --version
  npm --version
  ```

- ✅ **OpenAI API Key**
  
  从 https://platform.openai.com/api-keys 获取

## 🛠️ 详细步骤

### 1. 后端设置

```bash
# 进入后端目录
cd backend

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境 (Windows)
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
pip install autogen-agentchat autogen-ext[openai]

# 配置环境变量
copy env.example .env
# 然后编辑 .env 文件，填入您的 API Key

# 启动后端
python main.py
```

### 2. 前端设置

打开新的终端：

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

### 3. 访问应用

- 前端界面：http://localhost:3000
- 后端 API：http://localhost:8000
- API 文档：http://localhost:8000/docs

## 🎯 使用技巧

### 基本操作

1. **发送消息**：在输入框输入文本，按回车或点击发送
2. **查看回复**：AI 的回复会实时流式显示
3. **清空对话**：点击右上角"清空对话"按钮

### 高级功能

- **流式输出**：后端使用 SSE 协议，前端实时显示打字效果
- **响应式设计**：支持桌面和移动设备
- **主题定制**：可以在代码中修改渐变色和样式

## 🐛 常见问题

### 问题 1：后端启动失败

**错误**：`ImportError: No module named 'autogen_agentchat'`

**解决方案**：
```bash
cd backend
venv\Scripts\activate
pip install autogen-agentchat autogen-ext[openai]
```

### 问题 2：前端无法连接后端

**错误**：`Network Error` 或 `CORS Error`

**解决方案**：
1. 确保后端已启动并运行在 http://localhost:8000
2. 检查 `backend\.env` 中的 CORS 配置：
   ```env
   CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
   ```

### 问题 3：API Key 错误

**错误**：`Incorrect API key provided`

**解决方案**：
1. 检查 `backend\.env` 文件中的 API Key 是否正确
2. 确保 API Key 以 `sk-` 开头
3. 检查 API Key 是否有效且有足够额度

### 问题 4：端口被占用

**错误**：`Address already in use`

**解决方案**：

修改后端端口（`backend\.env`）：
```env
PORT=8001
```

修改前端端口（`frontend\vite.config.ts`）：
```typescript
server: {
  port: 3001,
  // ...
}
```

## 📊 性能优化

### 后端优化

1. 使用生产模式运行：
   ```bash
   uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
   ```

2. 启用缓存（可选）

### 前端优化

1. 构建生产版本：
   ```bash
   cd frontend
   npm run build
   ```

2. 使用生产服务器：
   ```bash
   npm run preview
   ```

## 🔐 安全建议

1. **不要提交 .env 文件**到版本控制
2. **使用环境变量**存储敏感信息
3. **定期更新依赖**以修复安全漏洞
4. **生产环境**请使用 HTTPS

## 📚 更多资源

- [完整文档](README.md)
- [前端文档](frontend/README.md)
- [后端文档](backend/README.md)
- [Ant Design X](https://x.ant.design/)
- [AutoGen](https://microsoft.github.io/autogen/)
- [FastAPI](https://fastapi.tiangolo.com/)

## 💬 获取帮助

如果遇到问题：

1. 查看 [常见问题](#-常见问题) 部分
2. 阅读详细的 [README.md](README.md)
3. 检查控制台错误信息

## 🎉 开始使用

现在您已经完成了所有设置，可以开始使用 AI 聊天系统了！

```bash
# 一键启动（Windows）
start_all.bat
```

祝您使用愉快！ 🚀

